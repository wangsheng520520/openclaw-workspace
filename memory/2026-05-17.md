# Pi Coding Agent 配置

## 当前配置

**更新时间**: 2026-05-17 20:37

### Pi 版本
- Pi: 0.74.0
- 位置: `~/.nvm/versions/node/v24.14.0/lib/node_modules/@earendil-works/pi-coding-agent`

### Pi 配置
- 配置文件: `~/.pi/agent/config.json`
```json
{
  "model": "deepseek/deepseek-chat",
  "baseUrl": "https://api.deepseek.com/v1",
  "apiKey": "sk-8bf…77e0"
}
```

### OpenClaw Pi 配置
- 配置文件: `~/.openclaw/openclaw.json` → `plugins.entries.acp-router`
```json
{
  "enabled": true,
  "dispatch": { "enabled": true },
  "backend": "acpx",
  "defaultAgent": "pi",
  "allowedAgents": ["pi"],
  "maxConcurrentSessions": 4
}
```

### 环境变量问题（已发现）
- `OPENAI_API_KEY` 被设置为 SiliconFlow 的 Key（错误）
- `OPENAI_BASE_URL` 被设置为 SiliconFlow 的端点
- 这导致 Pi 在 ACP 模式下可能使用错误的 Key

### 模型配置
- Pi 默认模型: `deepseek/deepseek-chat`
- 通过 OpenClaw 路由时使用 Gateway 的模型路由

## 问题记录

### minimax Provider 问题
- Pi 的 minimax provider 调用返回 401
- 直接 curl 测试 MiniMax API 成功
- 可能是 Pi 的 minimax 实现有兼容性问题

### 解决方案
- 继续使用 DeepSeek 作为 Pi 的默认模型
- DeepSeek API Key 有效且稳定

## 测试命令

```bash
# 测试 Pi ACP
pi acp "hello"

# 查看 Pi 版本
pi --version

# 列出可用模型
pi --list-models
```

## 已知问题

1. **minimax provider 401**: Pi 调用 minimax 时认证失败（待解决）
2. **环境变量冲突**: OPENAI_API_KEY 被设为 SiliconFlow Key（可通过显式传递 Key 解决）

## 相关文档

- Pi 安装位置: `/home/wszmd520520/.nvm/versions/node/v24.14.0/lib/node_modules/@earendil-works/pi-coding-agent`
- OpenClaw Pi 插件: `~/.openclaw/plugin-skills/acp-router/SKILL.md`
## acpx 配置确认 (22:13 - 22:23)

**用户确认**：acpx 是在 Gateway 升级到 v2026.5.7 之后配置的。

**当前 acpx 状态**：
- acpx 配置在 `~/.acpx/config.json`（需要检查）
- OpenClaw 通过 `plugins.entries.acp-router` 路由到 acpx backend
- 默认 agent: pi

**待测试**：
- `pi acp "hello"` — 验证 acpx 是否正常工作
- acpx 的 API key 配置是否正确

**系统状态**：
- Gateway: ✅ v2026.5.7
- MCPhub: ✅ 13/13 healthy
- memory-lancedb: ✅ 已修复（添加 SiliconFlow apiKey）
- acpx: ⚠️ 待验证

---