# Daily Memory - 2026-06-14

## Context & Memory System Maintenance

- Official-doc alignment task started: audit OpenClaw workspace context, memory_search, memory-lancedb, memory-wiki, active-memory, and Obsidian integration.
- Fresh finding: built-in `memory_search` vector search was paused because the index identity was built for provider `openai` while current config expects `openai-compatible`.
- Action: started official `openclaw memory index --force --agent main` rebuild after backing up `~/.openclaw/memory/main.sqlite*`.

