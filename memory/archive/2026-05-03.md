# 2026-05-03 记忆记录

## 会话活动（2026-05-02 晚 ~ 2026-05-03 凌晨）

### MCP 服务全面迁移到 Docker

**背景**: 用户已在 Docker 中部署 MCPhub (`samanhappy/mcphub`)，运行在 `localhost:3000`

**操作**: 将全部 14 个 MCP 服务从本地 npx 迁移到 `docker exec mcphub` 方式

| 阶段 | 操作 | 时间 |
|------|------|------|
| 初始 | 迁移 4 个服务 (playwright, exa, amap, markmap) | 23:27 |
| 全量 | 迁移所有 14 个服务到 Docker | 23:46 |
| 确认 | Gateway 重启成功 | 23:55 |

**效果**:
- 本地 MCP 进程数: 0
- 内存节省: ~1.5GB+
- 配置文件: `~/.openclaw/workspace/config/mcporter.json`
- 格式: `"command": "docker", "args": ["exec", "-i", "mcphub", "npx", "-y", "package"]`

**关键教训**:
- MCPhub 的 HTTP API (port 3000) 需要 Token 认证，不适合外部 MCP 连接
- OpenClaw 当前不支持 HTTP/SSE 模式的 MCP 传输
- `docker exec` 是连接 Docker 内 MCP 服务的最简方案

### 会话文件清理

**时间**: 2026-05-03 00:07

手动清理了 `~/.openclaw/sessions/` 中的残留文件:
- 删除 40 个 `.jsonl.bak-*` 备份文件
- 删除 25 个 `.jsonl.deleted.*` / `.trajectory.deleted.*` 标记文件
- 删除 374 个过期 `.trajectory-path.json` 路径引用
- 释放 57MB，文件从 535 降至 92

### 知识图谱 Cron 修复

**问题**: `知识图谱每日更新 v2` (cron 013cf5c6) 显示错误
- `graph.jsonl` 前 13 行 JSON 缺少闭合花括号（edit 工具截断）
- 导致后续 run 失败

**修复**:
- 重写 `graph.jsonl` 为完整有效的 30 行 JSONL
- Cron 任务升级到 v2.2：`edit` → `write` 覆盖写入
- 新增实体: `session_cleanup_20260503`, `docker_mcp_migration`, `miniMax_m2_7`, `system_health_20260501`

### 模型配额问题

- `qwen/qwen3.5-plus` 5小时配额耗尽（所有 Qwen dashscope 模型共享配额）
- 当前会话使用 `minimax/MiniMax-M2.7` 作为默认模型

### 已安装技能

- `using-superpowers@0.1.0` — meta-skill，工作流规范
- `desktop-control@1.0.0` — macOS 桌面自动化
- 安装教训: `openclaw skills install` 使用裸 slug（如 `using-superpowers`），非 author/slug 格式

## 2026-05-03 凌晨 — 模型统一 & 心跳检查

### 默认主模型统一为 minimax/MiniMax-M2.7

**时间**: 05:50~05:57

**变更**: 用户要求将所有任务统一使用主模型，不再分散配置

| 配置项 | 变更前 | 变更后 |
|--------|--------|--------|
| `agents.defaults.model.primary` | `qwen/qwen3.5-plus` | `minimax/MiniMax-M2.7` |
| 系统心跳 (heartbeat) | `volcengine-plan/ark-code-latest` | 移除显式 model（继承主模型） |
| 8 个 Cron 任务 | 各自显式 model | 全部移除 model 字段（继承主模型） |

**操作方法**:
- 直接编辑 `openclaw.json`，将 `primary` 改为 `minimax/MiniMax-M2.7`
- 逐一 `openclaw cron update` 移除每个任务的 `model` 字段（设为 `null` 效果不佳，改为不传 model）
- 最后在 `openclaw.json` 中移除 `agents.defaults.heartbeat.model`，使其继承 primary

**教训**: 
- cron update 传 `model: null` 会序列化为字符串 `"null"` 而非移除字段
- 需在 cron jobs.json 中手动删除 model 行
- HEARTBEAT.md 中的模型配置表格已过时，需更新为"全部继承主模型"

### 心跳检查 (05:57)

**QQ 邮箱** (4 封未读):
- 🚨 小米 MiMo 开放平台 — Token Plan 用量达 90%（05-02）
- 🚨 火山引擎 — 资源到期预警（05-02）
- ⚠️ GitHub — CI Run failed (OpenClaw repo, 05-02)
- ℹ️ 招商银行 — 贷款对账单 + 信用卡活动

**Gmail**: himalaya 超时（WSL2 网络抖动，已知问题）

**MCP 泄漏**: ✅ 0 进程（Docker 迁移生效），内存 3.8Gi/23Gi

**openclaw doctor**: 被 SIGKILL 终止

### SIGKILL 模式分析

**观察**: 多个并行 exec 命令被 SIGKILL:
- `dawn-hav`（Gateway 状态检查）
- `tide-dai`（Cron 状态检查）
- `calm-pra`（MCP Docker exec 验证）
- `keen-slu`（openclaw doctor）
- 只有 `neat-nud`（MCPhub Node OK）成功

**推测**: 凌晨时段资源竞争（内存压缩 + 心跳 + 并行 exec），可能触发 OOM Killer
- 内存 3.8Gi/23Gi 表面充足，但 WSL2 内存回收滞后
- `openclaw doctor` 可能触发较多子进程

### HEARTBEAT.md 更新需求

当前 HEARTBEAT.md 模型配置表格过时（仍在引用 volcengine-plan/ark-code-latest），需要更新为：
- 所有任务默认使用主模型 `minimax/MiniMax-M2.7`
- 无需分散配置，一改全改


## 2026-05-03 晚间 — Docker 精简 & 状态诊断

### 移除 MCPhub 中的 ziwei-doushu

**时间**: 21:23~21:51

**操作**:
- 确认 Docker MCPhub 中 ziwei-doushu 已删除（13 个 MCP 服务，原 14 个）
- 同步更新 mcporter.json，移除 ziwei-doushu 条目
- systemctl --user restart 使 Gateway 生效

**教训**: `gateway restart` 的 SIGKILL 只杀重启命令本身；用 `systemctl --user restart openclaw-gateway` 才能真正重启 Gateway 加载新配置。

### 知识图谱 Cron v2.2 确认

**时间**: 21:51

**状态**: v2.2 已于 05:50 升级（write 覆盖 + edit 已禁止），但 UI 显示的报错是 04:00 的 v2.1 旧运行残留。v2.2 明天 04:00 首次运行。
- `consecutiveErrors` 已清零，避免 UI 报警。

### SESSION-STATE 新鲜度诊断 (22:10)

**环境发现**: cron 子 agent 执行时 `$HOME` 被 WSL2 `/init` 重置为 `/root`，导致 `~/memory/` 解析到 `/root/` 而非用户 home。子 agent 的 workspace 正确但 home 错误——状态文件写入到错误路径。

另发现 cron spawn 时 `process.env.HOME` 未继承用户 HOME，可能影响所有 cron 任务的路径解析。

**三项问题诊断**:

| 问题 | 根因 | 状态 |
|------|------|------|
| heartbeat-state.json 写入卡在 03:00 | 心跳运行正常但状态文件写入逻辑失败 | 🔴 需要修复 |
| Dreaming 停摆 4 天 (04-29 后) | Memory Dreaming Promotion cron 运行但未生成新梦境 | 🔴 需要修复 |
| proactive-tracker.md 误报缺失 | 文件存在于 workspace 但子 agent 路径错误 | 🟡 路径问题 |

## 当日博客监控摘要

**05-03 早报**: 16 博客，37 篇新文章
- OpenClaw v2026.5.2 发布
- 五角大楼绕过 Anthropic 与其他 7 家科技巨头签约
- AI 生成演员和剧本无缘奥斯卡

**05-03 晚报**: 16 博客，50 篇新文章
- 马斯克 vs OpenAI 正式开庭
- 杭州法院裁定 AI 裁员违法（国内首例）
- DeepSeek V4 发布、苹果误打包 Claude.md

### 三项问题修复完成 (22:17~22:25)

**修复方法**:

| 问题 | 修复动作 | 文件 |
|------|---------|------|
| heartbeat-state.json 写入卡住 | 手动更新时间戳；HEARTBEAT.md 新增"步骤0：读取→修改→写回"指令 | `heartbeat-state.json` + `HEARTBEAT.md` |
| proactive-tracker.md 误报 | SESSION-STATE cron 升级 v2：所有文件路径改为绝对路径 | cron `53de5b3f` |
| Dreaming 停摆 05-01 起 | 标记 lastDreaming=05-01；识别为 Memory 技能内部机制问题，与 OpenClaw v2026.4.23 升级时间重合 | 待进一步调查 |

**额外清理**:
- HEARTBEAT.md 模型引用 `volcengine-plan/ark-code-latest` → `minimax/MiniMax-M2.7`
- 知识图谱 cron UI 错误状态已清除（旧 v2.1 运行残留，v2.2 明天生效）

### WSL2 HOME 环境变量发现 (22:10)

cron spawn 子 agent 时 HOME 未被正确继承（显示为 `/root` 而非 `/home/wszmd520520`），导致以 `~/` 开头的路径解析到错误目录。这是子 agent 找不到 proactive-tracker.md 等文件的根因。SESSION-STATE v2 修复通过**所有路径使用绝对路径**规避此问题。
