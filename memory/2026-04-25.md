# 2026-04-25 Daily Memory

## 会话摘要 (04-23 20:00 ~ 04-25 00:38)

### Evolver 进化审批
- 审批并固化进化运行 `run_1776922907970`（repair 类型，低风险）
- 内容：.gitignore、.npmignore、README.md 上游版本更新（MIT → GPL-3.0）
- Hub 授权通过，blast radius 72 文件超限但验证通过

### EvoMap 资产发布
- 成功发布 Ada Lovelace skill 到 EvoMap 市场
- Bundle ID: `bundle_f65cf8a824d925a8`，状态 `quarantine`（安全审查中）
- 3 个资产：Gene (poetical_science) + Capsule (ada_v2) + EvolutionEvent
- EvoMap 账户已关联 wszmd1793@gmail.com

### 博客监控修复 (两个问题)
1. **交互确认问题**：`blogwatcher read-all` 有 `[y/N]` 提示，非交互 exec 无法输入 → 改用 `--yes` 参数
2. **SQLite 数据库锁**：delete 日志模式并发冲突 → 切换到 WAL 模式 + busy_timeout 5000ms
- 修复后验证通过，84 篇文章扫描 + 13 篇标记已读正常

### 达尔文 Skill 全量评分
- 34 个 skill 完成基线评分（8 维度静态分析）
- 分布：🔴 <50 分 10 个 | 🟡 50-59 14 个 | 🟢 60-70 9 个 | 🏆 >70 1 个 (nuwa-skill 71.6)
- 全局短板：Checkpoint（检查点设计）和 Boundary（边界条件覆盖）

### 达尔文优化循环（5 个 Skill）
- **并行失败**：4 个并行子 agent → 500+ MCP 进程 → 12 GB 内存耗尽 → 全部超时
- **串行成功**：改用串行 + lightContext，每个 1-2 分钟，总计 ~6 分钟

| Skill | 改进前 | 改进后 | 提升 |
|-------|--------|--------|------|
| answeroverflow | 33.4 | ~70+ | +37 |
| weather | 40.7 | ~82 | +41 |
| nano-pdf | 41.7 | ~70+ | +28 |
| docker-essentials | 43.2 | ~75 | +32 |
| agent-browser | 43.7 | ~75+ | +31 |

### 关键经验
- **子 agent 并行 = MCP 进程风暴**：每个子 agent 启动 ~120 个 MCP 进程，4 并行 = 500+，直接耗尽内存
- **串行 + lightContext 是最优策略**：总耗时可控（6 分钟 vs 并行 20 分钟全超时）
- **beta MCP 修复范围有限**：只修了 isolated cron session，subagent 仍然泄漏
- darwin-skill 文件时间戳被 Evolver 刷新（04-23 13:28），原始发布时间 04-13 22:33

### 版本升级
- OpenClaw 从 `2026.4.22-beta.1` (e960878) → `2026.4.22` stable (00bd2cf)
- Gateway 已重启生效
- 主模型：ark-code-latest，fallback 首位：MiniMax M2.7

### 配置变更
- 主模型改回 volcengine-plan/ark-code-latest
- MiniMax M2.7 回到 fallback 首位

---
# 2026-04-25 Daily Memory

## 会话摘要 (04-23 20:00 ~ 04-25 00:38)

### Evolver 进化审批
- 审批并固化进化运行 `run_1776922907970`（repair 类型，低风险）
- 内容：.gitignore、.npmignore、README.md 上游版本更新（MIT → GPL-3.0）
- Hub 授权通过，blast radius 72 文件超限但验证通过

### EvoMap 资产发布
- 成功发布 Ada Lovelace skill 到 EvoMap 市场
- Bundle ID: `bundle_f65cf8a824d925a8`，状态 `quarantine`（安全审查中）
- 3 个资产：Gene (poetical_science) + Capsule (ada_v2) + EvolutionEvent
- EvoMap 账户已关联 wszmd1793@gmail.com

### 博客监控修复 (两个问题)
1. **交互确认问题**：`blogwatcher read-all` 有 `[y/N]` 提示，非交互 exec 无法输入 → 改用 `--yes` 参数
2. **SQLite 数据库锁**：delete 日志模式并发冲突 → 切换到 WAL 模式 + busy_timeout 5000ms
- 修复后验证通过，84 篇文章扫描 + 13 篇标记已读正常

### 达尔文 Skill 全量评分
- 34 个 skill 完成基线评分（8 维度静态分析）
- 分布：🔴 <50 分 10 个 | 🟡 50-59 14 个 | 🟢 60-70 9 个 | 🏆 >70 1 个 (nuwa-skill 71.6)
- 全局短板：Checkpoint（检查点设计）和 Boundary（边界条件覆盖）

### 达尔文优化循环（5 个 Skill）
- **并行失败**：4 个并行子 agent → 500+ MCP 进程 → 12 GB 内存耗尽 → 全部超时
- **串行成功**：改用串行 + lightContext，每个 1-2 分钟，总计 ~6 分钟

| Skill | 改进前 | 改进后 | 提升 |
|-------|--------|--------|------|
| answeroverflow | 33.4 | ~70+ | +37 |
| weather | 40.7 | ~82 | +41 |
| nano-pdf | 41.7 | ~70+ | +28 |
| docker-essentials | 43.2 | ~75 | +32 |
| agent-browser | 43.7 | ~75+ | +31 |

### 关键经验
- **子 agent 并行 = MCP 进程风暴**：每个子 agent 启动 ~120 个 MCP 进程，4 并行 = 500+，直接耗尽内存
- **串行 + lightContext 是最优策略**：总耗时可控（6 分钟 vs 并行 20 分钟全超时）
- **beta MCP 修复范围有限**：只修了 isolated cron session，subagent 仍然泄漏
- darwin-skill 文件时间戳被 Evolver 刷新（04-23 13:28），原始发布时间 04-13 22:33

### 版本升级
- OpenClaw 从 `2026.4.22-beta.1` (e960878) → `2026.4.22` stable (00bd2cf)
- Gateway 已重启生效
- 主模型：ark-code-latest，fallback 首位：MiniMax M2.7

### 配置变更
- 主模型改回 volcengine-plan/ark-code-latest
- MiniMax M2.7 回到 fallback 首位

---

## 04-25 凌晨~早间活动 (01:13 ~ 08:11)

### OpenClaw 升级到 v2026.4.23
- 从 `2026.4.22 (00bd2cf)` 升级到 **`2026.4.23 (a979721)`**
- `npm install -g openclaw@latest` 完成
- Gateway 已重启，Evolver 已重启

### Lobster 插件启用
- 用户要求启用 OpenClaw 内置的 Lobster 工作流引擎插件
- 功能：JSON-first 管道、作业调度、审批门控
- 来源：https://github.com/openclaw/lobster
- Gateway 重启加载新插件
- 当前插件总数：16 个

### Evolver 版本修复（重大发现）
- **问题**：`lifecycle.js` 启动的是本地 `skills/evolver/` 目录的旧 1.57.0 代码，而非全局 npm 安装的 1.69.21
- **原因**：`getRepoRoot()` 解析为 `__dirname/../..`，即本地目录
- **修复**：rsync 全局 1.69.21 代码到本地 skills/evolver/，保留 memory/assets/logs 数据
- **Git 仓库损坏**：rsync 覆盖了 `.git` 目录，导致 diff 无法生成
- **Git 修复**：重新 `git init` + 全量 commit，生成干净的 v1.69.21 基线 commit `0db56d9`
- 修复后状态：v1.69.21, Bridge=true, Validator=true
- **教训**：Evolver 的 lifecycle.js 使用相对路径定位仓库，全局安装不会自动更新本地工作目录

### 进化审查
- 拒绝了 2 个进化建议（`run_1776946927212` 和 `run_1777053101454`）
- 均为验证失败的修复尝试（validate-suite.js test suite exit code 1, ETIMEDOUT）
- 信号：tool_bypass, 验证失败重试

### 知识图谱更新
- 更新了 3 个实体的过时数据：
  - OpenClaw系统：v2026.4.23, 21 技能, 16 插件
  - Evolver：v1.69.21, Bridge/Validator 已启用
  - Ada：默认模型 ark-code-latest

### 知识图谱 cron 超时修复
- 04-25 04:02 执行超时（142s / 120s 限制）
- timeout 从 120s 放宽到 **300s**
- 验证：99s 成功完成

### lark-cli 安装
- 安装了飞书官方 CLI 工具 `@larksuite/cli` v1.0.19
- 命令：`lark-cli`，位于 nvm 全局 bin 目录
- **待配置**：OAuth Device Flow 认证
- 等待用户决定：复用现有飞书 app (cli_a911625db7f8dcc2) 还是新建 app

### Evolver PID 变化
- 12745 → 13981（版本修复后重启）→ 14589（Git 修复后重启）

### 关键经验
- **Evolver 本地 vs 全局版本差异**：lifecycle.js 用相对路径，全局安装不影响本地运行版本
- **rsync 会覆盖 .git**：同步代码时需要排除 `.git` 目录，或做好重建准备
- **知识图谱 cron timeout 需要充足余量**：MCP 启动耗时几十秒，120s 对于实际执行不够

---

## 04-25 晚间活动 (22:00 ~ 22:15)

### 模型配置更新

#### DeepSeek 配置
- API Key 配置完成：`sk-8bf487236f914cda8d9c8c59c61577e0`
- deepseek 插件已启用
- 将 `deepseek/deepseek-v4-flash` 加入 Fallback 链（第3位）
- 替换了原来的 `nvidia/google/gemma-4-31b-it`

#### 阿里云 Qwen 配置更新
- API Key 更换：`sk-sp-f90e2c8e51dc4e3c8e3d13e285121990` → `sk-c709b0f5ae3443cb92981dc7db1bb047`
- baseUrl 更换：`https://coding.dashscope.aliyuncs.com/v1` → `https://dashscope.aliyuncs.com/compatible-mode/v1`
- Gateway 已重启生效

### 当前 Fallback 链
1. `volcengine-plan/ark-code-latest` (主模型)
2. `minimax/MiniMax-M2.7`
3. `deepseek/deepseek-v4-flash` (🆕 替换 gemma-4-31b-it)
4. `qwen/qwen3.5-plus`
5. `qwen/qwen3-max-2026-01-23`
6. `volcengine-plan/kimi-k2.5`
7. `volcengine-plan/glm-5.1`

### 模型 Fallback 链扩展 (22:19 ~ 22:25)
- 新增 `qwen/qwen3.6-plus` 到 Fallback 第5位（1M 上下文，多模态，zero cost）
- 新增 `deepseek/deepseek-v4-pro` 到 Fallback 第4位（512k 上下文，三模态，maxTokens 32k）
- 当前 Fallback 链共 9 个模型（主模型 + 8 个 fallback）
